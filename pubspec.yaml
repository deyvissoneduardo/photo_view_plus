name: photo_view_plus
description: Photo View provides a gesture sensitive zoomable widget. Photo View is largely used to show interactive images and other stuff such as SVG.
version: 1.1.0
homepage: https://github.com/bluefireteam/photo_view_plus

environment:
  sdk: ">=3.1.0 <4.0.0"
  flutter: ">=3.14.5"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  test: ^1.25.2

flutter:

